let isLoggedIn = 1
let boolIsLogIn = Boolean(isLoggedIn)

console.log(boolIsLogIn)
console.log(isLoggedIn)

// **************** ops*************

console.log("1"+2);

console.log(+true);

// prefix and postfix operator
let x = 3;
let a = 3
const y = x++;
console.log(`x:${x}, y:${y}`);

//++a first increment then assign and a++ first assign then implement

const z = ++a;
console.log(`a:${a}, z:${z}`);
